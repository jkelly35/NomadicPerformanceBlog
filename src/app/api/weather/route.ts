import { NextRequest, NextResponse } from 'next/server'

export async function GET(request: NextRequest) {
  // Placeholder for weather API endpoint
  return NextResponse.json({ message: 'Weather endpoint not implemented yet' })
}
